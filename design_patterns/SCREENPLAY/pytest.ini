[pytest]
addopts = 
    --browser chromium
    --browser firefox
    --browser webkit
    --screenshot only-on-failure
    --video retain-on-failure
    --tracing off
    --numprocesses 4
    --output test-reports
    --alluredir=allure-results
    --clean-alluredir
testpaths = tests
